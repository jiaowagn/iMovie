<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_list_path(@list), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @list.title %></h2>
  <p><%= @list.profile %></p>
</div>
